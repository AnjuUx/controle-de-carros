import styled from "styled-components";
import variables from "@/variables";

const StyledDiv = styled.div`
    background-color: ${variables.colors.darkBlue}d0;
    z-index: 1;
    position: fixed;
    height: 100vh;
    width: 100vw;
`

export default StyledDiv